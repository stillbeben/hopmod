#!/bin/sh

if [ ! -p serverexec ]; then {
    echo "Apparently the server is not started."
    exit
}; fi

ttyname=`tty`
svstat=`echo printsvstatus [$ttyname] > serverexec`
echo -n "Server Status: $svstat"
sleep 1
