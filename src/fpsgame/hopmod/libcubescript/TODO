New file layout: significant classes in their own header and src files.

Rewrite unit tests using BOOST testing framework.

Asynchronous parse and eval interface.

Make library multithread safe.

Shared library build.

Unicode support.

Make domain class polymorphic.

Reseach for potential optimizations to domain's symbol table.

Refactor the infix alias assignment operator out of expression::eval into

Revise parse/print_type functions to wrap around boost::lexical_cast function.

Persist apply operations

