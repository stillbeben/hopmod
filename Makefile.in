BJAM=bin/bjam
USER_CONFIG="--user-config=user-config.jam"

all: .dummy
	@$(BJAM) release $(USER_CONFIG)

debug: .dummy
	@$(BJAM) debug $(USER_CONFIG)

clean: .dummy
	@$(BJAM) clean $(USER_CONFIG)

distclean: clean
	rm -rf Jamfile Makefile config.log

.dummy:
